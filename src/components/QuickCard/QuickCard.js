import React from "react";
import PropTypes from "prop-types";
//import { Test } from './QuickCard.styles';

const QuickCard = (props) => (
  <div className="QuickCardWrapper">
    Test content
  </div>
);

QuickCard.propTypes = {
  // bla: PropTypes.string,
};

QuickCard.defaultProps = {
  // bla: 'test',
};

export default QuickCard;
